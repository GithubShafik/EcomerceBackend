    // node server.js


//     const Turf = require('../models/Turf');
// const TurfEmployees = require('../models/TurfEmployees');
// const User = require('../models/User');
// const { getAllUsers } = require('./userController');

// // Create a new turf
// const createTurf = async (req, res) => {
//   if (req.user.role !== 'super_admin' && req.user.role !== 'turf_admin') {
//     return res.status(403).send({ message: 'Permission Denied' });
//   }

//   const { name, location, description ,username, email, password, role } = req.body;
//   const turf = new Turf({
//     name,
//     location,
//     description,
//     createdBy: req.user._id,
//   });

//   try {
//     await turf.save();

//     let user = await getAllUsers({email})

//     const turfemp = new TurfEmployees({
//       userID :user._id,
//       role,
//       turfID:turf._id
//     });
//     await TurfEmployees.save(turfemp)


//     res.status(201).send(turf);
//   } catch (error) {
//     res.status(500).send({ message: 'Error creating turf' });
//   }
// };

// // Get all turfs
// const getAllTurfs = async (req, res) => {
//   try {
//     const turfs = await Turf.find();
//     res.status(200).send(turfs);
//   } catch (error) {
//     res.status(500).send({ message: 'Error fetching turfs' });
//   }
// };

// // Get a single turf
// const getTurf = async (req, res) => {
//   try {
//     const turf = await Turf.findById(req.params.id);
//     if (!turf) {
//       return res.status(404).send({ message: 'Turf not found' });
//     }
//     res.status(200).send(turf);
//   } catch (error) {
//     res.status(500).send({ message: 'Error fetching turf' });
//   }
// };

// // Update a turf
// const updateTurf = async (req, res) => {
//   try {
//     const turf = await Turf.findById(req.params.id);

//     if (!turf) {
//       return res.status(404).send({ message: 'Turf not found' });
//     }

//     if (req.user.role !== 'super_admin' && turf.createdBy.toString() !== req.user._id.toString()) {
//       return res.status(403).send({ message: 'Permission Denied' });
//     }

//     const { name, location, description } = req.body;
//     turf.name = name || turf.name;
//     turf.location = location || turf.location;
//     turf.description = description || turf.description;

//     await turf.save();
//     res.status(200).send(turf);
//   } catch (error) {
//     res.status(500).send({ message: 'Error updating turf' });
//   }
// };

// // Delete a turf
// const deleteTurf = async (req, res) => {
//   try {
//     const turf = await Turf.findById(req.params.id);

//     if (!turf) {
//       return res.status(404).send({ message: 'Turf not found' });
//     }

//     if (req.user.role !== 'super_admin' && turf.createdBy.toString() !== req.user._id.toString()) {
//       return res.status(403).send({ message: 'Permission Denied' });
//     }

//     await Turf.findByIdAndDelete(req.params.id);
//     res.status(200).send({ message: 'Turf deleted successfully' });
//   } catch (error) {
//     res.status(500).send({ message: 'Error deleting turf' });
//   }
// };

// module.exports = { createTurf, getAllTurfs, getTurf, updateTurf, deleteTurf };
